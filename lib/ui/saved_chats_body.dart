import 'package:flutter/material.dart';

class SavedChatsBody extends StatelessWidget {
  const SavedChatsBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'This is saved chats Body!',
      ),
    );
  }
}
